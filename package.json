{
  "name": "css-talk",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch-styl": "stylus app/style.styl --watch --out  app/style.css",
    "serve": "browser-sync start --server app/  --files app/*.{css,html} --browser=false",
    "start": "npm run serve & npm run watch-styl"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.15.0",
    "stylus": "^0.54.5"
  }
}
